;(function( $, si, undefined ) {
	
	si.invoicingDepartments = {
		config: {
		},
	};

	/**
	 * methods
	 */
	si.invoicingDepartments.init = function() {
		// ...		
	};
	
})( jQuery, window.si = window.si || {} );

// Init
jQuery(function() {
	si.invoicingDepartments.init();
});
